export const sendVerificationEmail = async ({ to, verificationUrl, token, userName }) => {
  // Stub implementation - replace with your actual email sending logic
  console.log("Verification email would be sent to:", to);
  return Promise.resolve();
};

