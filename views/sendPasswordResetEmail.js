export const sendPasswordResetEmail = async ({ to, resetUrl, token, userName }) => {
  // Stub implementation - replace with your actual email sending logic
  console.log("Password reset email would be sent to:", to);
  return Promise.resolve();
};

